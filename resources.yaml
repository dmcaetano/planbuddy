# resources.yaml — what this project is, where it lives, what it needs.
# GENERATED by _tools/resources/gen_resources.py — re-run it any time.
#
# The `derived:` block is rebuilt from disk on every run. Do NOT hand-edit it.
# The `declared:` block is yours; the generator preserves it verbatim.

schema: 1
project: PlanBuddy
generated: '2026-09-07T17:54:20'
generator: _tools/resources/gen_resources.py
capability_matrix: _tools/resources/capabilities.yaml
derived:
  path: C:\Users\diogo\Dropbox\Projects\2026 Claude\PlanBuddy
  sdlc_class: SOLO
  summary:
    text: A memory-aware planner that turns one click into one grounded, decision-ready plan for a person,
      household, and pets.
    source: DESCRIPTION.md
  git:
    repo: true
    remote: https://github.com/dmcaetano/planbuddy.git
    branch: main
    head: a85c7883b5b5
    dirty_files: 4
    last_commit:
      date: '2026-09-07'
      sha: a85c788
      subject: 'sdlc: declare SDLC class in STATE.md'
    ahead: 1
    behind: 0
    upstream: origin/main
    sync: 1 unpushed commit
    matches_remote_tip: false
    remote_ref_age_note: ahead/behind computed against the last-fetched remote ref; run `git fetch` for
      a live answer
  version:
    source: package.json
    value: 1.1.5
    package_name: planbuddy
  stack:
  - node
  deploy:
    platforms:
    - render
    evidence:
    - platform: render
      marker: render.yaml
      where: .
    - platform: render
      marker: live URL in STATE.md
      where: .
      url: https://planbuddy.onrender.com
  docs:
    STATE.md: true
    DESCRIPTION.md: true
    LOG.md: true
    README.md: true
    CLAUDE.md: true
    AGENTS.md: true
    PROGRESS.md: true
  live_urls:
  - https://planbuddy.onrender.com
# ---------------------------------------------------------------------------
# declared: maintained by a human or an agent. The generator PRESERVES this
# block verbatim and never invents values for it.
#
#   purpose               one sentence: what this project is for
#   audience              who it is for (Diogo / a client / end users of <app>)
#   capabilities_required keys from _tools/resources/capabilities.yaml that this
#                         project actually needs (e.g. gmail_work, drive)
#   credentials           NAME + LOCATION only. Never put a secret value here;
#                         point at the file under "API Keys" that holds it.
#   canonical_checkout    if this repo is checked out more than once on disk,
#                         name the copy that feeds production.
# ---------------------------------------------------------------------------
declared:
  purpose: null
  audience: null
  capabilities_required: []
  credentials: []
  canonical_checkout: null
  notes: null
